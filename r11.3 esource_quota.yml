apiVersion: v1
kind: ResourceQuota
metadata:
  namespace: project1
  name: project1q
spec:
  hard:
    pods: "2"
    #requests.cpu: "1"
    #requests.memory: "1Gi"
    #limits.cpu: "2"
    #limits.memory: "2Gi"
    #requests.nvidia.com/gpu: 4

---
apiVersion: v1
kind: ResourceQuota
metadata:
  namespace: project2
  name: project2q
spec:
  hard:
    pods: "4"
    #requests.cpu: "1"
    #requests.memory: "1Gi"
    #limits.cpu: "2"
    #limits.memory: "2Gi"
    #requests.nvidia.com/gpu: 4    